services:
  - type: web
    name: angelbags-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:$PORT
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: WEBHOOK_URL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: ADMIN_CHAT_ID
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: IMGBB_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.12.7
    # Товары/заказы/вопросы/промокоды хранятся в Postgres (DATABASE_URL), а
    # фото товаров — на бесплатном фотохостинге ImgBB (IMGBB_API_KEY).
    # Локальный диск контейнера ни для чего из этого не используется, так что
    # платный Persistent Disk на Render больше не нужен вообще.
